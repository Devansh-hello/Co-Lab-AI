# In development the Vite proxy forwards /api and /ws to localhost:5000 automatically.
# Only set these for staging or production deployments.
# VITE_API_URL=https://your-backend.com/api/v1
# VITE_WS_URL=wss://your-backend.com/ws
